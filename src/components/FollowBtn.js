const FollowBtn = () => {
  return (
    <button
      style={{
        outline: "2px solid var(--btnBgColor)",
        color: "var(--btnBgColor)",
        padding:".3em 1em" ,
        border: "none",
        fontSize:"1em",
        borderRadius: "2em",
        fontWeight: "bold",
      }}
    >Follow</button>
  )
}

export default FollowBtn